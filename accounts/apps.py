"""App configuration for the accounts app."""

from django.apps import AppConfig


class AccountsConfig(AppConfig):
    """Holds the custom user model and the profile model."""

    name = "accounts"
